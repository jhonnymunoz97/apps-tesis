import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aqsystems.recolector_usuario',
  appName: 'urban_conductor',
  webDir: 'www',
  bundledWebRuntime: true
};

export default config;
